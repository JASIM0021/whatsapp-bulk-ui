# Production Backend URL (used for SSE endpoints that bypass Vercel proxy)
# IMPORTANT: Must be HTTPS to avoid mixed-content blocking from Vercel's HTTPS frontend
VITE_BACKEND_URL=https://bulksenderapi.todayintech.in
